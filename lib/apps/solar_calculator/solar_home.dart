import 'package:flutter/material.dart';

class SolarHome extends StatelessWidget {
  const SolarHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Solar System Calculator Content'));
  }
}
